'use strict'

class Pass{
    // a voir si on met l'utilisateur ici 
    constructor(type, description, price, beginDate, endDate){
        this.type = type;
        this.description = description;
        this.price = price;
        this.beginDate = beginDate;
        this.endDate = endDate;
    }

    //update infos ? 

}

module.exports = Pass;